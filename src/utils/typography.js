import Typography from "typography"

const typography = new Typography({
  baseFontSize: "16px",
  baseLineHeight: 1.45,
  headerFontFamily: [
    "Source Sans Pro",
    "-apple-stylem",
    "Helvetica",
    "sans-serif",
  ],
})

export default typography
